import styled from 'styled-components';

export const CheckDetails = styled.span`
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  color: #fff;
  font-size: 16px;
  text-align: center;
  background: #000;
  padding: 8px;
  border-radius: 4px;
`;
